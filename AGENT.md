# AGENT.md

Agent 行为约束与规则，所有 AI Agent 必须严格遵守。

## 数据库保护

**绝对禁止删除或清空数据库。**

- 不得执行 `rm AiChatApp/chat.db` 或任何删除 `.db` 文件的命令
- 不得执行 `DROP TABLE`、`DELETE FROM`（无 WHERE 条件）等破坏性 SQL
- 不得调用任何会清空或重建数据库的操作（如无条件 `EnsureDeleted()`）
- 如需重置数据库，必须**明确获得用户的逐字确认**后方可执行，且须提前备份

数据库中存储着用户的聊天记录、记忆、技能和项目数据，一旦删除无法恢复。
